import React, { Component } from "react";

export default class UserGoals extends Component {
  render() {
    return (
      <div>user is logged in, show message to user, see goals, add goals</div>
    );
  }
}
